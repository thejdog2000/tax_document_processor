{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the Tax Document Processor spike.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
